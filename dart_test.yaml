# E2Eテスト(test/e2e/)は fediverse_e2e 環境が必要。
# 実行: RUN_E2E=1 dart test test/e2e -j 1 (環境が無い場合は自動スキップ)
# -j 1 必須: 並列実行するとレート制限に当たる。fediverse_e2e の
# `make test-e2e` / `make test-e2e-docker` も -j 1 を付けている
tags:
  e2e:
    timeout: 3m
